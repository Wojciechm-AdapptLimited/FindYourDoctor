@page "/manage-specializations"

@using FindYourDoctor.Services

@inject DiseaseService DiseaseService
@inject IDialogService DialogService

<MudTable Items="_specializations" Hover="true" FixedHeader="true" SortLabel="Sort by" Height="700px" Filter="new Func<SpecializationTableObject, bool>(FilterSpecializations)">
    <ToolBarContent>
        <MudText Typo="Typo.h6">Manage Specializations</MudText>
        <MudIconButton Icon="@Icons.Material.Filled.Add" OnClick="AddSpecialization"/>
        <MudSpacer />
        <MudTextField @bind-Value="_searchString" Immediate="true" Placeholder="Search" Adornment="Adornment.Start" 
                      AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0" />
    </ToolBarContent>
    <ColGroup>
        <col/>
        <col/>
        <col style="width: 15%"/>
    </ColGroup>
    <HeaderContent>
        <MudTh><MudTableSortLabel SortBy="new Func<SpecializationTableObject,object>(x => x.Id)" Enabled="true">Id</MudTableSortLabel></MudTh>
        <MudTh><MudTableSortLabel SortBy="new Func<SpecializationTableObject,object>(x => x.Name)">Name</MudTableSortLabel></MudTh>
        <MudTh>Actions</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd>@context.Id</MudTd>
        <MudTd>@context.Name</MudTd>
        <MudTd>
            <MudIconButton Icon="@Icons.Material.Filled.Edit" OnClick="() => EditSpecialization(context.Id)"/>
            <MudIconButton Icon="@Icons.Material.Filled.Delete" OnClick="() => DeleteSpecialization(context.Id)"/>
        </MudTd>
    </RowTemplate>
    <PagerContent>
        <MudTablePager />
    </PagerContent>
</MudTable>